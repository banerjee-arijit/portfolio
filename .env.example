# GitHub Personal Access Token (PAT) for GraphQL API v4 contributions query
# 1. Create a fine-grained token at https://github.com/settings/tokens
# 2. Grant read access to public repositories and user profile data.
# 3. Rename this file to .env or .env.local and insert your token below:
VITE_GITHUB_TOKEN=""
